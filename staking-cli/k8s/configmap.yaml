apiVersion: v1
kind: ConfigMap
metadata:
  name: event-listener-config
  namespace: monad-staking
data:
  # Monad RPC configuration (WebSocket URL)
  rpc_url: "wss://testnet-rpc.monad.xyz"

  # Database configuration (non-sensitive)
  db_host: "monad-events-listener-postgres-service"
  db_port: "5432"
  db_name: "monad_events"
